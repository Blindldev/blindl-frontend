import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.simpledating.app',
  appName: 'Simple Dating App',
  webDir: 'build'
};

export default config;
